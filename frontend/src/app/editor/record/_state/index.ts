import recordData from "./recordDataSlice";
import recordDisplay from "./recordDisplaySlice";
import recordState from "./recordStateSlice";

const recordReducers = {
  recordData,
  recordDisplay,
  recordState,
};

export default recordReducers;

import formulaData from "./formulaDataSlice";
import formulaDisplay from "./formulaDisplaySlice";
import formulaState from "./formulaStateSlice";
import chemicalDisplay from "./chemicalDisplaySlice";
import chemicalData from "./chemicalDataSlice";

const formulaReducers = {
  formulaData,
  formulaDisplay,
  formulaState,
  chemicalDisplay,
  chemicalData,
};

export default formulaReducers;

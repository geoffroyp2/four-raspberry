import targetData from "./targetDataSlice";
import recordData from "./recordDataSlice";
import graphDisplay from "./graphDisplaySlice";
import recordDisplay from "./recordDisplaySlice";
import targetDisplay from "./targetDisplaySlice";

const graphReducers = {
  targetData,
  recordData,
  graphDisplay,
  recordDisplay,
  targetDisplay,
};

export default graphReducers;

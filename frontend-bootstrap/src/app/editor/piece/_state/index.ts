import pieceData from "./pieceDataSlice";
import pieceDisplay from "./pieceDisplaySlice";
import pieceState from "./pieceStateSlice";

const pieceReducers = {
  pieceData,
  pieceDisplay,
  pieceState,
};

export default pieceReducers;

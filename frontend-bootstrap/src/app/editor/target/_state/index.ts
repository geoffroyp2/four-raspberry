import targetData from "./targetDataSlice";
import targetDisplay from "./targetDisplaySlice";
import targetState from "./targetStateSlice";

const targetReducers = {
  targetData,
  targetDisplay,
  targetState,
};

export default targetReducers;
